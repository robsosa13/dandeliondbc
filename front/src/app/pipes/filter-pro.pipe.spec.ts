import { FilterProPipe } from './filter-pro.pipe';

describe('FilterProPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProPipe();
    expect(pipe).toBeTruthy();
  });
});
